const altura = 5; // Altura de la pirámide invertida

for (let i = altura; i >= 1; i--) {
    const asteriscos = 2 * i - 1;

    // Imprimir asteriscos
    for (let j = 0; j < asteriscos; j++) {
        document.write("*");
    }

    document.write("<br>");
}
