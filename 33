const tamaño = 8; // Tamaño del tablero de ajedrez

for (let i = 0; i < tamaño; i++) {
    for (let j = 0; j < tamaño; j++) {
        if ((i + j) % 2 === 0) {
            document.write("*");
        } else {
            document.write("&nbsp;");
        }
    }
    document.write("<br>");
}
