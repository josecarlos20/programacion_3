// Crear un array vacío
let numerosAleatorios = [];

// Obtener el valor de N del usuario
let n = parseInt(prompt("Introduce la cantidad de números aleatorios que deseas generar:"));

// Generar N números al azar y guardarlos en el array
for (let i = 0; i < n; i++) {
  let numeroAleatorio = Math.floor(Math.random() * 100); // Cambia el rango si lo deseas
  numerosAleatorios.push(numeroAleatorio);
}

console.log(numerosAleatorios); // Mostrar el array de números aleatorios
